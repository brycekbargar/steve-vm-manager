'use strict';

const VM = require('./../index.js');

describe('For the ChucK VM', () => {
  beforeEach('Setup VM', () => {
    this.chuck = new VM();
  });
  it('expect it to be ok!', () => {
  });
});
